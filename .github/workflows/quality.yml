name: Quality Gate

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 1 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    name: Lint, test, typecheck, and build
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out the exact revision
        uses: actions/checkout@v6

      - name: Use Node.js 22
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Report content maintenance
        run: npm run content:status -- --github-summary

      - name: Report staging media inventory
        run: npm run media:staging -- --github-summary

      - name: Run the complete quality gate
        run: npm run check

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high
