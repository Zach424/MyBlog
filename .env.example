# Optional. Leave empty to derive the public origin from Vercel request headers.
NEXT_PUBLIC_SITE_URL=

# Local-only GitHub OAuth app credentials for testing /studio.
# Production values belong in Vercel Project Settings > Environment Variables.
GITHUB_OAUTH_ID=
GITHUB_OAUTH_SECRET=
