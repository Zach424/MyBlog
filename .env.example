# Optional. Leave empty to derive the public origin from Cloudflare proxy headers.
NEXT_PUBLIC_SITE_URL=

# Local-only GitHub OAuth app credentials for testing /studio.
# Production values belong in GitHub Environment and Cloudflare secrets.
GITHUB_OAUTH_ID=
GITHUB_OAUTH_SECRET=
